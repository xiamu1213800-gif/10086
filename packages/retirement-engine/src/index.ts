import type{CashflowPoint,RetirementInput,RetirementPlan}from"@retirement/shared";const mr=(a:number)=>Math.pow(1+a,1/12)-1;export function calculateRetirementPlan(i:RetirementInput):RetirementPlan{let assets=i.currentAssets,retirementAssets=0,sustainableAge=i.lifeExpectancy;const cashflow:CashflowPoint[]=[];let month=0;for(let age=i.currentAge;age<=i.lifeExpectancy;age++){const r=age>=i.retirementAge,months=age===i.lifeExpectancy?1:12,rate=mr(r?i.postRetirementReturn:i.preRetirementReturn),expense=i.retirementMonthlyExpense*Math.pow(1+i.inflationRate,Math.max(0,age-i.retirementAge));for(let m=0;m<months;m++){const income=r?i.pensionMonthlyIncome:i.monthlySavings,cost=r?expense:0;assets=assets*(1+rate)+income-cost;month++;if(age===i.retirementAge&&m===0)retirementAssets=Math.max(0,assets);if(assets<=0&&r)sustainableAge=Math.min(sustainableAge,age)}cashflow.push({age,year:month/12,phase:r?"retirement":"work",assets:Math.max(0,assets),income:r?i.pensionMonthlyIncome*12:i.monthlySavings*12,expense:r?expense*12:0,netCashflow:r?(i.pensionMonthlyIncome-expense)*12:i.monthlySavings*12})}const years=Math.max(1,i.lifeExpectancy-i.retirementAge),real=Math.max(-.02,i.postRetirementReturn-i.inflationRate),gap=Math.max(0,i.retirementMonthlyExpense-i.pensionMonthlyIncome),capital=gap*12*(real===0?years:(1-Math.pow(1+real,-years))/real)+i.medicalReserve;return{retirementAssets,requiredRetirementCapital:capital,fundingRatio:retirementAssets/Math.max(1,capital),sustainableAge,cashflow}}